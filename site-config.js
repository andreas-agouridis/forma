'use strict';

// Optional public configuration. Never put a personal API key or access token here.
window.FORMA_CONFIG = Object.freeze({
  // Register a Pollinations App Key to give the connection screen your app's name.
  // An empty value uses the provider's supported device connection without attribution.
  pollinationsClientId: '',
  // Use publish.html to stamp the real URL into static HTML and generate discovery files.
  canonicalUrl: ''
});
